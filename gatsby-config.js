module.exports = {
  siteMetadata: {
    title: `Gatsby Default Starter`,
  },
  plugins: [
    `gatsby-plugin-react-helmet`,
  ],
  // Note: it must *not* have a trailing slash.
  pathPrefix: `/sitetest-public/2018`,
}
